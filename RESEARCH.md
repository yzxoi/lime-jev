# lime 本地上下文候选重排调研

> 这是实现之前的调研快照。后续已完成本地安装与模型测试；当前结果和默认方案见 [验证记录](docs/VALIDATION.md)。

调研日期：2026-09-23。结论：工程上可行，不需要调用 Jev 云端 API。最短路径是先确认 lime 的上下文与候选覆盖，再增加一个可替换的本地重排器；Laya 多语言版 + MLX 值得首先验证，但尚无证据证明它能改善中文输入法首选率。

本轮完成源码检查和公开资料核验，没有安装推理依赖、下载模型、调用 Jev、修改现用输入法或进行推理实测。工作目录原本为空；调研源码下载到临时目录。检查的上游快照为 lime `fb1ebdbe0744766a7a7395f218d1305d2e920664`、Laya `839784f7c9b45139e0fb7ef60051ae9c3c9bab16`、zhihz/openjev `ff94f61ec3b2d6a71b4c2b01e76104dfe37da4e6`，不代表用户目前安装的版本。本机只读取了硬件信息：Apple M3 Max，64 GiB 内存。

**需求应定义为：在符合当前拼音的候选中，用可靠的当前上下文选出更好的第一候选。**

例如，预期“汽车 + youxiang”优先“油箱”；“我现在 + youxiang”可能优先“又想”。第二个例子本身不是唯一确定答案，“我现在邮箱……”也能构成句子；因此不能把所有歧义输入都当成必有唯一正确词。模型应协助排序，用户仍决定上屏。

需要分别解决三个问题：正确文字是否已经出现在候选中、上下文是否真的是当前光标前的文字、模型是否能把正确候选排在前面。重排器只直接解决第三个问题。

**lime 已有本地模型与上下文，先排查原有链路。**

上游默认使用 Qwen3-0.6B 的 IQ4_XS GGUF，Deno/TypeScript + node-llama-cpp 推理；当前源码显式设置 `gpu: false`。模型会根据提交文字计算下一 token 的分布，再按拼音筛选，已有上下文能力。[模型加载与初始化](https://github.com/xushengfeng/lime/blob/fb1ebdbe0744766a7a7395f218d1305d2e920664/main.ts#L55)

源码中的几个关键事实：

| 位置 | 已确认行为 | 对本需求的影响 |
|---|---|---|
| `main.ts:266` 的 `commit()` | 将提交文字写入模型上下文 | 应先检查“汽车”“我现在”是否在候选请求前正确进入上下文 |
| `main.ts:444`、`:687` | 中间处理与最终排序有长词优先策略 | 最终顺序不完全由语义概率决定；但“油箱/邮箱/又想”长度相同，不能据此断定这就是该例根因 |
| `main.ts:478` 开始的长句补全 | 从首个路径逐步延伸，未枚举全部多 token 组合 | 某个同音组合可能根本不在候选中，单纯重排无法补出它 |
| `main.ts:100`、`:689` | 已有 `afterReSort`，但同步且只接收候选 | 接入异步本地模型仍需扩展接口，或在服务端返回候选前增加一步 |
| `rime/lua/llm_pinyin.lua:65`、`:87` | 发送提交历史及部分预编辑文字 | 不等于自动获得任意应用的完整编辑框上下文 |
| `rime/lua/fetch_text.lua:37` | `io.popen(curl)` 后阻塞读取结果 | 额外推理直接增加候选等待时间，必须控制时限 |

默认的常用字重排还会保留前四项，再调整后面的候选，不能笼统说成它覆盖了第一候选。[配置](https://github.com/xushengfeng/lime/blob/fb1ebdbe0744766a7a7395f218d1305d2e920664/config.ts#L14)、[常用字排序](https://github.com/xushengfeng/lime/blob/fb1ebdbe0744766a7a7395f218d1305d2e920664/tools/resort_feq.ts)

当前服务端只创建一个 runner。结合客户端上报内容，删除已上屏文字、移动光标、切换输入框后存在上下文失配风险；源码没有提供完整的应用与编辑位置同步。这是静态检查发现的局限，尚未在用户当前版本复现。[客户端](https://github.com/xushengfeng/lime/blob/fb1ebdbe0744766a7a7395f218d1305d2e920664/rime/lua/llm_pinyin.lua)、[服务端](https://github.com/xushengfeng/lime/blob/fb1ebdbe0744766a7a7395f218d1305d2e920664/server.ts#L27)

**可本地运行的方案。**

| 方案 | 已有能力 | 对本项目的判断 |
|---|---|---|
| Laya multilingual + laya-mlx | 322M 多语言决策模型，Apple Silicon 原生 MLX；给候选列表，返回选择和分数 | 首个轻量验证对象；中文同音词效果必须单独测 |
| zhihz/openjev | 冻结 Qwen3-4B-Instruct-2507，MLX 8-bit，从候选标签 logits 读取分布 | 可作较大模型对照；现有延迟数据不适合直接挂在每次按键上 |
| SemIf，原 OpenJev，openjev.com | 本地开放模型读取候选标签概率，提供浏览器演示 | 方法可借鉴；没有必要为桌面输入法引入整个浏览器演示 |
| 在 lime 中改进已有模型评分 | 已有模型、拼音索引和概率接口 | 实现增量小，应作为基线；扩大模型或改变评分方法的效果仍需验证 |

Laya 应选 `convaiinnovations/laya-multilingual`，MLX 移植有已转换的 `aac6fef/laya-multilingual-mlx` 权重。英语基础版及基于英语编码器的 typed-decisions 版不宜作为中文候选选择的默认模型。[Laya 模型说明](https://github.com/NandhaKishorM/laya)、[MLX 移植](https://github.com/mizorewww/laya-mlx)、[转换权重](https://huggingface.co/aac6fef/laya-multilingual-mlx)

MLX 项目发布的详细基准在 M3 Max、40 核 GPU、128 GiB 上，多语言 FP16 单个约 91-token 短请求的 P50/P95 为 **10.91/19.48 ms**；1024-token 满上下文 P50 为 **43.50 ms**。权重约 **614 MiB**，短请求 MLX 分配峰值约 **687.6 MiB**，不等于完整进程内存。README 另列 7.39 ms，与详细表不同，故不把 7 ms 当成保证。以上都是作者数据，不含模型加载，也不是这台机器的输入法端到端实测。[详细基准](https://github.com/mizorewww/laya-mlx/blob/main/BENCHMARKS.md)

Laya 的核心风险是任务泛化。上游“Honest limits”承认基础模型在部分未专门训练的决策任务上接近随机水平；项目收录的 2026-09-21 中文工作流诊断中，多语言版四选一为 20/64。后者是合成工作流、历史快照，不能换算成输入法准确率，但足以说明“支持中文”不等于“擅长中文同音词”。[模型局限](https://github.com/NandhaKishorM/laya#honest-limits)、[中文诊断及边界](https://github.com/NandhaKishorM/laya/blob/main/research/benchmarks/feishu_zh/README.zh-CN.md)

zhihz/openjev 明确支持中文本地推理，每题 2–8 个候选。其文档给出的 Apple M3、16 GB、八次短 HTTP 请求中位数为 **533 ms**，MLX 分配峰值约 **4.60 GB**；这不是与 Laya 同输入、同机器的对比。项目标准准备流程还涉及约 8 GB 原权重与 4 GB 转换副本。按现有证据，不选它作为每次按键的默认路径。[OpenJev 文档](https://github.com/zhihz/openjev)

“OpenJev”有多个互不相同的项目，不能只按名字选用。[SemIf](https://openjev.com/)说明其为独立本地实验，候选归一化分数不是经过验证的正确率；[openjev.sh](https://openjev.sh/)描述的是托管 Jev 访问，并不满足本地推理目标。没有必要为了本需求复刻完整 Jev API。

**建议接入方式：在原候选返回前做一次有时限的本地选择。**

流程为：Rime → lime 生成候选 → 获取当前会话的可靠文字前缀 → 本地选择器 → 将所选候选移动至第一位 → Rime 展示。

第一版建议明确这些边界：

1. 输入为文字前缀、当前拼音、候选 ID 与候选文本；模型只返回已有候选 ID 和分数。先选取覆盖相同拼音范围的 5–8 个候选，避免把只消耗 `you` 的“有”与完整消耗 `youxiang` 的“油箱”直接竞争。
2. 移动完整候选对象，保留 `consumedkeys`、`preedit`、`remainkeys`，所选词移至首位，其余维持相对顺序；不重复插入相同条目，不自动上屏。
3. 先检查候选召回。如果“又想”不存在，补充合法的多 token 候选路径或词典候选，再谈重排；不能让选择模型自由生成一个可能不符拼音的词。
4. 模型常驻、预热，使用本机回环服务。缓存键包含会话/上下文版本、拼音及候选集合，避免上下文变了仍复用旧结果。
5. 先用短前缀（如最近 64–128 字，待测）和少量完整候选。重排增加的 P95 延迟可暂定以 30 ms 为目标、50 ms 为回退预算；这些是设计目标，不是已达成结果。无有效上下文、服务失败或超时则保留原列表。
6. 现有 Lua 请求是同步的。第一版采用服务端有时限等待；不能只在服务端开个后台任务就宣称候选框会自行刷新。真正的异步刷新需要另外改前端。
7. 为上下文建立独立状态，不直接把模型内部上下文 token 当作编辑框全文；内部可能包含初始化提示和候选补全过程。第一版限定于可靠的连续输入，编辑/切换场景应清空或降级。获取任意应用的光标周边文字是独立的系统集成工作。
8. 不使用随意设定的“置信度 > 0.8”作为可靠性保证；用输入法专用验证集决定何时置顶和何时保留原排序。

从代码改动量看，首个可评估版本可以在 `server.ts` 的 `await single_ci(...)` 后加独立重排调用，保留现有同步 `afterReSort`；同时补上可靠的会话文字状态。实现成熟后再决定是否合并为引擎内的异步插件接口。这比实现完整 Jev 协议更直接。

**下一步应做小型对照实验，模型选择由结果决定。**

建议准备约 100–200 条中文同音输入，包含短语、连续上下文、词组与多 token 组合、不同候选顺序和有意保留的歧义例。标注可接受答案集合，别强行给模糊例唯一标签。先测原 lime，再测上下文/排序修正基线、Laya multilingual MLX；若后者不够好，再试本地 Qwen 4B 的候选标签评分或候选续写概率，必要时才考虑专用微调。

同时报告：正确词是否进入候选池、首选命中率、原本正确却被改错的比例、候选换序后的稳定性、增量与完整链路 P50/P95、冷启动以及模型出错时能否保持原候选。候选池覆盖率决定重排所能达到的上限。

本轮的推荐是：**先检查 lime 的上下文与候选覆盖，以 Laya multilingual + MLX 做轻量对照；验证有效后再将它接为默认首选重排器。完全本地运行有现成路径，但“选词更准”仍待实测。**
